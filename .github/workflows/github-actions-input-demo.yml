name: Github Actions Input Demo

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  workflow_call:

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "inputs.allowed_features = ${{ inputs.allowed_features }}"
        shell: bash